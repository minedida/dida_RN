import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  dark: false,
  colors: {
    ...DefaultTheme.colors,
    primary: '#607edf',
    accent: '#607edf',
    placeholder: '#c3c3c3'
  }
}

export default theme
